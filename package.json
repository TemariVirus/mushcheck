{
  "scripts": {
    "init": "npm ci && cd app && npm ci && for dir in ../api/*/; do [ -f \"$dir/package.json\" ] && (cd \"$dir\" && npm ci); done"
  },
  "dependencies": {
    "mysql2": "^3.6.0",
    "node-html-parser": "^6.1.5"
  }
}
